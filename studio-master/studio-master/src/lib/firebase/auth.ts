
// This file's functions are no longer needed for the auth-free flow.
// We are keeping the file but commenting out its contents to avoid breaking any imports.
// In a real project, you would delete this file and its usages.

/*
import { 
  createUserWithEmailAndPassword, 
  signInWithEmailAndPassword,
  signOut as firebaseSignOut,
  updateProfile as firebaseUpdateProfile,
  type User as FirebaseUser,
  type AuthError,
  sendPasswordResetEmail,
} from 'firebase/auth';
import { auth } from './config';
import { createPerson } from './firestore';
import { UserRole, type UserProfile, PersonStatus } from '@/types';
import { doc, collection } from 'firebase/firestore';
import { db } from './config';
import { splitName } from '@/lib/utils';
*/
