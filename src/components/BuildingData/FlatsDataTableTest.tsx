import React from 'react'

const FlatsDataTableTest: React.FC = () => {
  return (
    <div className="space-y-6">
      <h2>Flats Test - Simple Version</h2>
      <div className="bg-white p-4 rounded border">
        <p>This is a test component to check if the issue is in FlatsDataTable specifically.</p>
        <p>If you can see this, the routing works fine and the issue is in the main FlatsDataTable component.</p>
      </div>
    </div>
  )
}

export default FlatsDataTableTest
